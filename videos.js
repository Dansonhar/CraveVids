/* ─────────────────────────────────────────────────────────────
   Descriptions for the videos in  videos/

   The page lists every video file it finds in the videos/ folder
   on its own. This file only adds the words around them.

   Key = exact filename.  Anything left out falls back to the
   filename as the title, with no description.

       "opening-reel.mp4": {
         title: "Opening Reel",
         desc:  "One or two sentences about the video.",
         notes: ["Short bullet", "Another short bullet"],
       },

   Order below = order on the page (files not listed come after,
   sorted by name).
   ───────────────────────────────────────────────────────────── */
const VIDEO_META = {
  // ← descriptions go here once the video files are in videos/
};
